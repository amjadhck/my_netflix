import 'package:flutter/material.dart';

class HotandNewScreen extends StatelessWidget {
  HotandNewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("HotandNewScreen"),
      ),
    );
  }
}
